# 当前状态：等待下一次游戏更新

更新日期：2026-09-22。机器状态见 `project.json`。

- 当前没有活动翻译批次，没有预设修改源，`patch_v2/` 不存在。
- 当前原文基线：`snapshots/baseline_20260922/`，含8712个官方三语及零协文本文件，逐文件 SHA-256 已记录。
- 实际可读取的游戏标识：Steam build **25345468**。游戏显示版本未知，不把示例 v1.0 当真实版本。
- 对应零协包：**2026092102**。该版本号只标识包来源，不独立证明每个字段的语义正确或游戏兼容性。
- 上一轮129个补丁文件已归档到 `archives/20260922_previous_patch/`，哈希保持不变。
- 发布仓库已建立：<https://github.com/haool871/limbus-zh-10ch>（本地 `../limbus-zh-10ch`），
  只放补丁本体与安装说明，`patch/` 当前为空（零协已覆盖全部文本）。发布流程见
  [版本更新流程 §6.1](workflow/版本更新流程.md)。旧的 `Limbus-zh-CN-patch` 保留作为
  知识库分发镜像，不再往它的 `patch/` 发布新批次。

流程和 skill 已按 [修复记录](workflow/流程修复记录_20260922.md) 更新。唯一现行操作入口为 [版本更新流程](workflow/版本更新流程.md)。游戏更新后先保存新快照、比较两版原文，再确认范围并创建独立批次；历史缺口和旧补丁不自动加入。

准备阶段不需要创建空翻译目录。`doctor.py` 报准备状态，译文验收在没有批次时明确“不适用”。

本次修复没有改动游戏安装中的译文、上一轮仓库 `patch/` 或已有发布 ZIP，也没有提交推送或安装新补丁。历史交付物不代表当前活动工作区。
